# ZakatFlow — Project Plan

This file is used by Lovable to track implementation context. It does not contain sensitive information.

## Architecture

- **Frontend:** React + Vite + Tailwind CSS + TypeScript
- **Backend:** Lovable Cloud (Supabase-based) with Edge Functions
- **Encryption:** Hybrid Managed-Sovereign model (AES-256-GCM via JWE)
- **License:** AGPL-3.0

## Environment Setup

See `.env.example` for required environment variables. The `.env` file is auto-managed by Lovable Cloud and excluded from version control via `.gitignore`.
